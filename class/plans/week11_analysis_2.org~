* data analysis with pandas II
** review last class
- loading a text: DataFrame and read_csv
- viewing text methods
  - especially: info(), columns, syntax for accessing columns
- sorting:
  - df.value_counts('column') -> counts highest values per column
  - df.sort_values('column') -> sorts column, try with year
    in descending order
- filtering:
  - df['column'] == 'text string'
    - new_df = (df['column'] == 'text string')
    - df[new_df] -> returns new dataframe with condition

** analysis II
- more sorting:
  - df.sort_values(by=['column_name', 'column_name'], ascending=[True,
    False])
  - string methods
    - new_df = df['column'].str.contains('string text')
- visualizing
  - df.department.value_counts().plot(kind = 'barh')

** 
